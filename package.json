{
  "name": "project-chaos",
  "private": true,
  "workspaces": [
    "packages/*"
  ],
  "devDependencies": {
    "eslint": "^8.21.0",
    "turbo": "^1.4.2",
    "typescript": "^4.7.4"
  },
  "scripts": {
    "build": "yarn workspace @chaos/core build && yarn workspace @chaos/lottery-draw build && yarn workspace @chaos/lottery-draw-ui build"
  }
}
